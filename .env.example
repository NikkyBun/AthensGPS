# Copy this file to .env.local and fill in your Convex deployment URL.
# `npx convex dev` prints this URL (it looks like https://<name>-<n>.convex.cloud).
# The EXPO_PUBLIC_ prefix is required so Expo inlines it into the app bundle.
EXPO_PUBLIC_CONVEX_URL=
